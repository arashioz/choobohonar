import SeoWorkspaceShell from "@/components/seo/SeoWorkspaceShell";

export default function SeoWorkspaceLayout({ children }: { children: React.ReactNode }) {
  return <SeoWorkspaceShell>{children}</SeoWorkspaceShell>;
}
